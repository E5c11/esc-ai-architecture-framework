# ESC AI Engineering Framework

See [`INSTRUCTIONS.md`](./INSTRUCTIONS.md) for how to use this framework.

This file exists only because Claude Code auto-loads a file with this name — it
intentionally holds no content of its own, so the real instructions stay
provider-agnostic and have exactly one copy.
