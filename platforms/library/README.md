# platforms/library/

Guidance for Kotlin Multiplatform libraries that are consumed independently of the
repository that produces them. Library documents cover API compatibility, variant
packaging and publication rather than application architecture.

## Document ID prefix: `PLAT-LIB-`

