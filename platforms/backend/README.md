# platforms/backend/

Kotlin / Spring Boot / JPA implementation guides.

Documents here extend `architectures/backend-service/` with backend-specific
tooling. They assume the reader has already loaded the relevant architecture layer.

## Tech stack covered

- Spring Boot
- Spring Security
- Spring Data JPA
- Hibernate
- PostgreSQL / MySQL

## Document ID prefix: `PLAT-BE-`
